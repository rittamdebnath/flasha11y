Compare the same screen "{{screenName}}" across {{deviceCount}} devices: {{devices}}.

Evaluate:
1. Responsive adaptation — does the layout adapt appropriately per device?
2. Content parity — is critical content preserved across sizes?
3. Touch target scaling — are interactive elements sized correctly per device?
4. Typography scaling — does text scale appropriately?
5. Navigation adaptation — does navigation adjust for each form factor?

Flag elements that break, disappear, or become unusable at different sizes.
